<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BTL"
        tools:targetApi="31">
        <activity android:name=".UI.startApp"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.home" />
        <activity android:name=".UI.signUp"/>
        <activity android:name=".UI.login"/>
        <activity android:name=".UI.SignUpSuccessfully"/>
        <activity android:name=".UI.forgotPass"/>
        <activity android:name=".UI.userProfile"/>
        <activity android:name=".UI.employee"/>
        <activity android:name=".UI.ShowEmployee"/>
        <activity android:name=".Dialog.dialog"/>
        <activity android:name=".UI.Work"/>
        <activity android:name=".UI.EmployeeManager"/>
        <activity android:name=".UI.TimeKeeping"/>
        <activity android:name=".UI.AddWork"/>
        <activity android:name=".UI.Evaluate"/>
        <activity android:name=".UI.DetailEvaluate"/>
        <activity android:name=".UI.AddEmployee"/>
    </application>

</manifest>